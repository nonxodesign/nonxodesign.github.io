$(function($){
                $('.container_project').Chocolat({
                    imageSize: 'default',
                    loop: false,
                    overlayOpacity : 0.9,
                    duration: 500,
                    fullScreen:false
                    // lastImage:0
                });

            });